const cartCount=document.getElementById('cartCount');const toast=document.getElementById('toast');const search=document.getElementById('foodSearch');const categoryGrid=document.getElementById('categoryGrid');const dishCards=[...document.querySelectorAll('.dish-card')];let count=Number(cartCount.textContent||0);let activeCategory='all';function showToast(message){toast.textContent=message;toast.classList.add('show');window.clearTimeout(showToast.timer);showToast.timer=window.setTimeout(()=>toast.classList.remove('show'),1500)}document.querySelectorAll('.add-btn').forEach(button=>{button.addEventListener('click',()=>{count+=1;cartCount.textContent=count;showToast('Added to cart')})});function filterDishes(){const q=(search?.value||'').trim().toLowerCase();dishCards.forEach(card=>{const matchesText=card.dataset.name.includes(q);const matchesCategory=activeCategory==='all'||card.dataset.category===activeCategory;card.classList.toggle('hidden',!(matchesText&&matchesCategory))})}search?.addEventListener('input',filterDishes);categoryGrid?.addEventListener('click',event=>{const button=event.target.closest('.category-card');if(!button)return;categoryGrid.querySelectorAll('.category-card').forEach(item=>item.classList.remove('active'));button.classList.add('active');activeCategory=button.dataset.category||'all';filterDishes()});document.getElementById('subscribeForm')?.addEventListener('submit',event=>{event.preventDefault();event.currentTarget.reset();showToast('Subscribed successfully')});